# 线上配置文件
spring:
  application:
    name: user-center
    # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://47.115.225.119:3306/user_center?useSSL=false
    username: root
    password: root
  session:
    timeout: 86400
  # session 失效时间
server:
  address: 0.0.0.0

